console.log('Hello from node')

/**
 * Module is an encapsulate and reusable chuck of code that has its own context
 * In Node.js each file is treated as its own module
 * Types of Moudle:
 * 1. Local/userDefined module
 * 2. Build in module
 * 3. Third party module
 */